module.exports = {
	useTabs: true,
	semi: false,
	singleQuote: true,
	trailingComma: 'all',
	bracketSpacing: false,
	printWidth: 120,
	arrowParens: 'avoid',

	// Note that the sibling .editorconfig file provides further overrides that
	// Prettier honors (uses as overrides).
}
